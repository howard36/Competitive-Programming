#include <bits/stdc++.h>
using namespace std;

int a[5000], b[5000];

int main() {
	int n;
	scanf("%d", &n);
	for (int i = 0; i < n; i++) {
		scanf("%d", &a[i]);
	}
	for (int i = 0; i < n; i++) {
		scanf("%d", &b[i]);
	}
    for (int i = 0)
}